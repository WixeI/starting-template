//If you want to add more variables to MUI theme instead of just overriding existing ones, add their types here
export interface CustomTheme {
  palette: {
    primary: {
      main: string;
    };
    secondary: {
      main: string;
    };
  };
}
