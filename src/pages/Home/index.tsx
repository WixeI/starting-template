import Button from '../../global/components/Button';
import * as S from './styles';

const Home = () => {
  return (
    <S.Wrapper>
      <Button variant="solid">Teste</Button>
    </S.Wrapper>
  );
};

export default Home;
